import { EntradaComponent } from './entrada.component';
import { Route } from '@angular/router';

export const EntradaRoutes: Route[] = [
    {
        path: 'entrada',
        component: EntradaComponent
    }
];
