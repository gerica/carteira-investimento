import { ListaComponent } from './lista/lista.component';
import { Route } from '@angular/router';

export const SaidaRoutes: Route[] = [
    {
        path: 'lista-saidas',
        component: ListaComponent
    }
];
