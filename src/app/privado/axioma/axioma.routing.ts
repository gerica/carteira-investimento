import { AxiomaComponent } from './axioma.component';
import { Route } from '@angular/router';

export const AxiomaRoutes: Route[] = [
    {
        path: 'axioma',
        component: AxiomaComponent
    }
];
